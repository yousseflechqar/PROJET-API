SET FOREIGN_KEY_CHECKS=0;

mappedBy reference an unknown target entity property -> le nom f mappedBy khass ykon bhal name field w machi bhal nom de la table column
spring not detected -> delete tomcat server w add it again


marcheDto.societes = new ObjectMapper().valueToTree(stes);

//			List<Integer> societes = new ObjectMapper().convertValue(bean.societes, ArrayList.class);


int default value 0 not null
int don't deal with NULL


java.nio.file.AccessDeniedException -> append file to directory


The message java.lang.NoSuchMethodException: xxx.<init>() means that someone tried to call a constructor without any parameters. 
Adding a default constructor should solve this problem